=> link_to t('order.create'), new_order_path, remote: true, class: 'btn btn-success btn-raised'
=> link_to t('customer.create'), new_customer_path, remote: true, class: 'btn btn-info btn-raised'

.panel.panel-primary
  .panel-heading
    h2.panel-title
      = t('order.list')
  .panel-body#orders
    = render @orders
